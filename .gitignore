# Оригинальные изображения (храним только оптимизированные версии в git)
# Оригиналы остаются локально для возможного редактирования

# Все PNG файлы в папках img и preview (оригиналы)
projects/*/img/*.png
projects/*/preview/*.png

# Исключение: если вдруг будут оптимизированные PNG
!projects/*/img/*-optimized.png
!projects/*/preview/*-optimized.png
